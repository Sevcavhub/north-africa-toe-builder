# Railway deployment configuration for Flask API
# This tells Railway to deploy ONLY the Flask app in scripts/battlegroup/web/

[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = [
    "pip install -r scripts/battlegroup/web/requirements.txt"
]

[start]
cmd = "cd scripts/battlegroup/web && python app.py"
