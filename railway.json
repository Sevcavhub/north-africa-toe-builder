{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r scripts/battlegroup/web/requirements.txt && mkdir -p database && python scripts/database/restore_equipment_table.py"
  },
  "deploy": {
    "startCommand": "cd scripts/battlegroup/web && python railway_app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
